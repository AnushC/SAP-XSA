ID: CPL166MTA
_schema-version: '2.0'
description: Just a smart application
version: 0.0.1

modules:
 - name: cpl166db
   type: hdb
   path: cpl166db
   requires:
    - name: hdi_cpl166db

resources:
 - name: hdi_cpl166db
   properties:
      hdi-container-name: ${service-name}
   type: com.sap.xs.hdi-container
